{
  "name": "meteo-rappel-aeration",
  "version": "1.0.0",
  "description": "Cloudflare Worker — surveille la température extérieure (Open-Meteo) et envoie une notification push (ntfy) au bon moment pour aérer.",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.6.2",
    "wrangler": "^3.78.0"
  }
}
